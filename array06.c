#include<stdio.h>
#include<limits.h>
int main()
{
	int number[5], i, min, max;
	
	printf("%d %d", INT_MIN, INT_MAX);
	/*
	for(i=0; i<5 i++)
	{
	     printf("다섯 번 입력 하세요[%d 번] : ",ㅑ+1 );
		 scanf("%d , &number[i]);
    }
    for(i=0; i<5 i++)
        printf("number[%d] = %d\n",i); 
        
    */
    
    return 0;
		    
}
